from setuptools import setup, find_packages

setup(
    name = 'minimal_authenticator',
    version = '1.0.0',
    url = 'https://github.com/tomcatling/minimal_authenticator',
    author = 'Author Name',
    author_email = 'tomcatling@gmail.com',
    description = '-',
    packages = find_packages(),    
    install_requires = [],
)